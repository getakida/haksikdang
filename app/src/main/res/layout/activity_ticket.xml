<!-- res/layout/activity_ticket.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/qr1"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:layout_marginBottom="8dp"
        android:text="← 메뉴"
        android:textSize="25dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="16dp"
        android:text="식권 보관함"
        android:textSize="30sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnAvailableCoupons"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/colorPrimary"
            android:text="사용 가능" />

        <Button
            android:id="@+id/btnUsedCoupons"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="사용 완료" />

    </LinearLayout>

    <!-- 식권 버튼이 추가될 레이아웃 -->
    <LinearLayout
        android:id="@+id/ticketContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- 초기에 보여질 식권 버튼들을 추가합니다 -->
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="스파게티 식권"
            android:id="@+id/ticketButton1"
            android:onClick="onTicketButtonClick" />



        <!-- 여기에 필요에 따라 다른 초기 식권 버튼을 추가할 수 있습니다 -->

    </LinearLayout>


</LinearLayout>
